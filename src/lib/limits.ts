/** Field limits shared by the share form (client) and the API validator (server). */
export const MAX_VOTER_NAME = 80;
export const MAX_EXPLANATION = 500;
export const MAX_SUMMARY = 500;
export const MAX_VOTER_IMAGE_URL = 2048;
